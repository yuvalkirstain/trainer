# @package _global_

defaults:
  - /experiment/clip

output_dir: outputs/clip_h_lr_${optimizer.lr}_gsteps_${accelerator.max_steps}_dist_${criterion.is_distributed}

model:
  pretrained_model_name_or_path: laion/CLIP-ViT-H-14-laion2B-s32B-b79K
