# @package _global_

output_dir: vanilla_clip_large_outputs

model:
  pretrained_model_name_or_path: openai/clip-vit-large-patch14

dataset:
  dataset_name: data_diff/datasets/PickaPic_reward.ds
  batch_size: 8