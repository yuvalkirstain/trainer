# @package _global_

output_dir: outputs/${accelerator.project_name}/clip

dataset:
  dataset_name: /home/olab/kirstain/trainer_dev/data_all/datasets/PickaPic_reward.ds
  batch_size: 4

accelerator:
  mixed_precision: BF16
  project_name: reward_all