# @package _global_

defaults:
  - /experiment/clip

output_dir: outputs/clip_g_lr_${optimizer.lr}_gsteps_${accelerator.max_steps}

model:
  pretrained_model_name_or_path: laion/CLIP-ViT-g-14-laion2B-s12B-b42K