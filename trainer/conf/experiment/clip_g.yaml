# @package _global_

defaults:
  - /experiment/clip

output_dir: outputs/${accelerator.project_name}/clip_g_lr_${optimizer.lr}_gsteps_${accelerator.max_steps}

model:
  pretrained_model_name_or_path: laion/CLIP-ViT-bigG-14-laion2B-39B-b160k